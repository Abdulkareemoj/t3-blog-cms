export interface Categories {
  categories: Categories[];
  slug: string;
  categoryName: string;
  category: string;
}

export interface CategoryDetails {
  categories: Categories[];
  slug: string;
  categoryName: string;
}

export interface CategoryPosts {
  posts: Posts[];
  createdAt: string;
  slug: string;
  title: string;
  featuredImage: {
    url: string;
  };
}

export interface CategoryPostsDetails {
  posts: Posts[];
  createdAt: string;
  slug: string;
  title: string;
  featuredImage: {
    url: string;
  };
}

export interface CategoryPostsDetails {
  posts: Posts[];
  createdAt: string;
  slug: string;
  title: string;
  featuredImage: {
    url: string;
  };
}

export interface CategoryPostsDetails {
  posts: Posts[];
  createdAt: string;
  slug: string;
  title: string;
  featuredImage: {
    url: string;
  };
}
